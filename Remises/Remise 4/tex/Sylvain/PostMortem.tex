%\section{Points forts du robot}
%\label{s:PointsForts}

%L'algorithme A* de trajectoire, l'analyse itérée de plus d'une image de la Caméra Monde pour s'assurer du bon nombre de trésors et d'îles rendant la détection de ceux cis  la plus constant possible, les calibrations pour déterminer les couleurs s à détecter sur chaque table et le choix des couleurs, formes et calibrations pour les différents objets ajouter par l'équipe pour détecter le robot, la station de base rendent certainement le robot robuste contre les erreurs diverses.

%\pagebreak

%\section{Améliorations potentielles}
%\label{s:Amelioration}

%Bien que l'équipe a choisi une démarche simple dans la conception de l'asservissement et l'implémentation des déplacements en général, il y a des ajustements envisageables qui changeraient la rapidité d'exécution. Pour l'instant, le robot tourne sur lui-même d'abord pour obtenir une orientation et se déplace ensuite vers l'avant, vers l'arrière, vers la gauche et vers la droite. Par contre, il ne peut pas se déplacer en oblique ou en diagonale. La figure \ref{f:DeplacementDevantCirculaire} suivante démontre le type de déplacement que le robot effectue :

%\begin{figure}[htp]
%   \centering
%   \includegraphics[width=1\textwidth]{fig/AvancementCirculaireEtAvant.png}
%   \caption{Déplacement du robot présentement}
%   \label{f:DeplacementDevantCirculaire}
%\end{figure}

%\pagebreak

%Comme amélioration, le robot pourrait se déplacer en diagonale également. Il pourrait se déplacer avec des commandes "x, y" peu importe l'orientation du robot. Par exemple, si le robot a une position (100, 100), peu importe son orientation, voulant se déplacer vers la position (200, 200) et qu'il n'y a pas la présence d'obstacle, il devrait simplement se déplacer en ligne droite sans avoir à changer son orientation. La figure \ref{f:AvancementDeCote} suivante présente la trajectoire améliorée envisageable du robot. On remarque que le robot serait plus rapide passant de 4 tâches à 2 dans ce cas ci :

%\begin{figure}[htp]
%   \centering
%   \includegraphics[width=1\textwidth]{fig/AvancementDeCote.png}
%   \caption{Déplacement du robot permettant les directions diagonales}
%   \label{f:AvancementDeCote}
%\end{figure}

%Un aspect un peu négatif du robot se situe par rapport à ses déplacements courts. Ne pas faire d'erreurs sur la trajectoire était l'objectif, c'est pourquoi même sur des lignes droites, le robot avance sur de plus courtes distances afin de s'assurer d'envoyer une correction plus rapidement pour les prochains points à atteindre. Avec un meilleur asservissement et une meilleur certitude sur l’alignement par rapport à la trajectoire, il faudrait prôner de plus grandes distances sur les différents déplacements, surtout en ligne droite.

%Également, L'algorithme de trajectoire trouve le chemin le plus court pour l'instant, mais il pourrait trouver le chemin le plus sécuritaire, c'est à dire passer le plus loin des îles, des murs, des trésors, etc. Il y a déjà un petit ajustement afin de passer entre deux îles de façon plus sécuritairement, mais il 
